[package]
name = "pong"
version = "0.1.0"
authors = ["dongle"]
edition = "2026"

[dependencies]
agb = "0.22.6"

[profile.dev]
opt-level = 3
debug = true

[profile.release]
opt-level = 3
lto = "fat"
debug = true
